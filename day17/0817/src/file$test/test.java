package file$test;

import java.io.File;
import java.io.IOException;

public class test {
    public static void main(String[] args) throws IOException {
        File file = new File("/Users/charlie/Desktop/aaa/ddd/aa.txt");
//            file.createNewFile();  创建文件、目录
//            file.mkdirs();         根据路径创建多级文件夹（不能新建文件）
//            file.delete();         删除指定文件或空文件夹
//            file.exists();         一个文件（目录）是否存在
//            file.isDirectory();    判断指定路径下对象是否是文件夹
//            file.isFile();         判断指定路径下对象是否是文件
//            file.length();         获取文件的大小（字节数），不可以过去文件夹的
//            file.isHidden();       判断文件（目录）是否隐藏
//            file.canRead();        判断文件（目录）是否可读
//            file.canWrite();       判断文件（目录）是否可写
//            file.canExecute();     判断文件（目录）是否可执行
//            file.getName();        获取文件（目录）的名字
//            file.getPath();        获取文件（目录）的相对路径
//            file.getPathAbsolute();获取文件（目录）的绝对路径
//            file.lastModified();   获取文件（目录）上次修改的时间（毫秒数）
//            file.list();           获取目录下的所有文件（目录）名字
//            file.listFiles();      获取目录下的所有文件（目录）封装成对象


    }
}
